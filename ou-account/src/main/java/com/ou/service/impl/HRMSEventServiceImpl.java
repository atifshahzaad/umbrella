package com.ou.service.impl;

import org.springframework.stereotype.Service;

@Service
public class HRMSEventServiceImpl /* implements HRMSEventService */ {

	/*
	 * @Value("${ou_account.topic.userCreated}") private String userCreatedTopic;
	 * 
	 * @Autowired private KafkaTemplate<String, UserCreatedEvent> kafkaTemplate;
	 * 
	 * @Override public void sendUserCreatedEvent(UserCreatedEvent event) {
	 * kafkaTemplate.send(userCreatedTopic, event);
	 * 
	 * }
	 */

}
